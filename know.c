#include<stdio.h> 
int main(){   
//   a) int a = 8^8 is !=  64 here, ^ is xor (bitwise op)  // valid
/* b).int x;int y = x; //valid
but int x ,int y =x;  // not valid

when we make char so we have to input only 1 char 
Ex. char == "**"  // not valid


*/

return 0;
}