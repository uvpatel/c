#include<stdio.h> 
int main(){   
//Conditional Operator
/*Ternary
Syntax :
Condition? doSomething if TRUE:doSomething if FALSE;
? check your conditon is true or not
if true print : before otherwise  after :
*/
//usage

  int age;
  printf("Enter age: ");
  scanf("%d",&age);
  age >= 18? printf("Adult  \n") : printf("not Adult\n");
// not more usage but synatax is good if u want to perform some small conditional so u can use it and do not terminate beetween

return 0;
}